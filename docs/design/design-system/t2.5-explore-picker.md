# T2.5 — W-1 Explore picker wireframe record

Status: **drawn proposal awaiting reviewer validation and Rob's ruling**. This record and
`t2.5-explore-picker.html` are one artifact. The PNGs are deterministic captures of the HTML at
the variants named below.

## Constraint structure — enumerated before figures

1. **Discovery scope is one persisted state set:** visible categories, include-hidden,
   show-saved, and coverage shading. Its default is all categories visible, hidden OFF, saved ON,
   coverage ON (P2R-5).
2. **List-map filtering is a different state set:** `TracksVisitFilter` narrows visits in an
   active list map by loved state, cross-list membership, and category. It is not silently
   re-labelled as discovery scope (OF4).
3. **The category grid is the shared control seam:** in an active list map,
   `ListMapLayerVisibility.displayed/updating` already maps the grid to the list visit filter's
   category set. Category controls are not duplicated.
4. **All state edits are live, reversible, and immediately legible.** There is no grouped-edit
   mode and no filled Apply action. The sanctioned grouped-edit fallback remains available only
   if validation finds the drawn flow mis-tappy.
5. **Clear scope is one quiet verb:** when any effective map-scope state is non-default, it resets
   the four persisted discovery choices and, in list-map context, the active list visit filter.
   It is absent when there is nothing to clear. This makes the one-tap exit truthful in every
   state that can thin the current map.
6. **The Explore door's indicator follows effective scope:** the active state is true when the
   persisted discovery scope differs from default or a contextual list visit filter is active.
   The visible text `Scope set` and the accessibility value `Scope adjusted` make the state
   non-colour-only.
7. **Scope rows remain switch rows:** P2R-4 ratified the A8 switch-row geometry wholesale for
   this row class. R15's filled-pill + filled-glyph morphology governs state clusters; a switch
   row communicates ON/OFF through knob position and track shape as well as colour. Replacing the
   switch would discard ratified geometry without a gate (OF5).
8. **Search renders nothing.** Settings and About remain navigational quiet rows with full
   44pt targets. The Scope chips are reversible state controls and may use R10 tiling.
9. **The surface states under review are complete as a set:** door/default, door/scope-set,
   picker/default, picker/adjusted, picker/list-context, and picker/AX. The default frame pins the
   ruled order and defaults; the other frames prove the indicator, Clear-scope exit, OF4 answer,
   and Dynamic Type behavior.

## Adopted figures and their qualifiers

| Element | Figure drawn | Provenance and qualifier |
|---|---|---|
| Canvas | 390×844 | W-1 commission; every phone frame is exact. |
| Sheet | 22pt top radius; 36×4 grabber; 6/14/14 padding | Ratified component-metrics row, source `ia-doors`. |
| Title / subtitle | 26pt Newsreader 600 / 14pt sans | Ratified component-metrics row, source `ia-doors`. AX uses the measured A8 stress treatment (41/22pt), not a new system metric. |
| Default Scope switch rows | 52pt minimum; 15pt/600 label; 20×20 icon; 10pt gap; 6/2 padding; 44×28 switch, 22pt knob at 3/19pt; 1pt hairlines | **RATIFIED row-class geometry**, P2R-4. |
| AX Scope switch rows | 86pt minimum; 23pt/600 label; 30×30 icon; 14pt gap; 10/2 padding; 51×31 switch, 25pt knob at 3/23pt | **RATIFIED row-class geometry**, P2R-4. |
| Default Scope block rhythm | 18pt header-to-label; 11pt label-to-chips; 14pt chips-to-controls | **MEASURED**, source A8 frozen render at `f492296e932d8f3225362875f466f6d40504fe54`. OF2's default applies: the rhythm does not travel as ratified row-class law. |
| AX block rhythm | 14/9/14pt around the AX Scope block | Carried inside the P2R-4-ratified AX Scope-control table row. |
| Default chips | 22pt visible height; 6pt horizontal and row gap | Visible geometry is the ratified `ia-doors` chip row. The **wrapped** flow is W-1's choice; this drawing preserves the shipped 6pt flow. |
| Default chip targets | 3pt toward each distinct-action neighbour; 11pt at free run edges | R10: `min(11pt, 6pt/2) = 3pt`. Interior vertical target equals the 28pt row pitch; horizontal targets meet at the 3pt half-gap and never overlap. The 44pt free-space bound applies only at run edges/margins. |
| AX chips | 38pt visible minimum; 8pt horizontal and row gap | A8 measured stress treatment retained as the W-1 wrapped-flow choice. |
| AX chip targets | 4pt toward each distinct-action neighbour; 11pt at free run edges | R10: `min(11pt, 8pt/2) = 4pt`. Interior vertical target equals the 46pt row pitch. |
| Clear scope | 44pt full-width target | Quiet, reversible momentary verb. It does not need target tiling; the full floor is retained. |
| Settings / About | 44pt minimum each | Navigational controls: R10's rider forbids reduced/derived clearance. |
| `Scope set` indicator | 60×18pt visual badge inside a 44pt Explore door | Non-interactive state evidence inside the door target. Text + filled sliders glyph + presence/absence carry state; colour is not the sole signal. |

Snow-token contrast in every captured frame: ink/surface **14.02:1**, muted/surface **5.38:1**,
and accent-contrast/accent **6.13:1**. The HTML evidence panels report live DOM bounding boxes
beside each rendered phone; these table figures are the design inputs, not a substitute for those
measured outputs.

## OF4 — list-mode merge answer

Three approaches were considered:

1. **Contextual merge (drawn):** keep the discovery rows stable, let the existing bridge make the
   category grid context-sensitive, and add a `Visits in this list` state-chip group only while a
   list map is active. This preserves the two state models without leaving a second Scope surface.
2. **One undifferentiated chip field:** mix discovery, loved, list-membership, and category state
   under one label. Rejected because it hides which domain a control changes.
3. **Keep the floating list chips:** rejected because #469 explicitly requires one Scope surface
   and the overlay would remain a second entry point.

The drawn contextual group contains `Loved` and one chip per selectable cross-list membership.
The active list itself is context, not a redundant filter chip. Category choices use the main
category grid. All updates are live, so the old filter sheet's Apply/Close controls retire and
P2R-7 remains true.

## P2R-7 proof record

> this surface does not pair a filled action with a state cluster; the R16 proof obligation travels to the first surface that does.

## Identifier annex — A1 convention

The convention is `explore.scope.<control>`, matching the landed
`explore.row.settings` / `explore.row.about` ownership boundary. T2.8 performs this contract
rename once, in one commit, with nothing else in flight.

| Current contract | W-1 target contract | Disposition |
|---|---|---|
| `map.layers.show-hidden` | `explore.scope.include-hidden` | Relocated Scope switch row. |
| `map.layers.show-saved` | `explore.scope.show-saved` | Relocated Scope switch row. |
| `map.layers.coverage-shading` | `explore.scope.coverage-shading` | Relocated Scope switch row. |
| `map.layers.show-all-categories` | `explore.scope.categories.toggle-all` | Relocated quiet category verb. |
| `map.layers.category.<id>` | `explore.scope.category.<id>` | Relocated category state chip. |
| — | `explore.scope.clear` | New P2R-5 quiet Clear-scope row. |
| `map.list-mode.filter.loved` and `track-filter-picker.loved` | `explore.scope.list-visits.loved` | One contextual state chip replaces both entry points. |
| `track-filter-picker.list.<id>` | `explore.scope.list-visits.list.<id>` | Contextual cross-list membership state chip. |
| `map.list-mode.filter.category.<id>` and `track-filter-picker.category.<id>` | `explore.scope.category.<id>` | Folded through the existing category bridge; no duplicate control. |
| `map.list-mode.filter.lists` | — | Aggregate launcher retires; individual list states render in the contextual group. |
| `track-filter-picker.category.all` | `explore.scope.categories.toggle-all` | The shared category verb is the replacement. |
| `track-filter-picker.apply`, `track-filter-picker.close`, `track-filter-picker.sheet` | — | Grouped-edit sheet retires; state updates are live. |

The Explore door keeps `map.door.explore`; its accessibility value becomes `Default scope` or
`Scope adjusted`. Settings and About keep `explore.row.settings` and `explore.row.about`.

## Capture variants

| Variant | Output | Repeat SHA-256 |
|---|---|---|
| `?variant=picker-default` | `t2.5-explore-picker-default.png` | `8290952339f4e41411e04a753296dd8eda0df14e874e7ad354be572d3aec5747` |
| `?variant=picker-adjusted` | `t2.5-explore-picker-adjusted.png` | `7df2e04fc8afe3472b5125d7d90304cf9d61b290829a9646d8a416a51cd12a04` |
| `?variant=picker-list` | `t2.5-explore-picker-list.png` | `feccba42867e7b52539135940a84c3f7e238fe68ed5db5ee706e8e159c617c13` |
| `?variant=picker-ax` | `t2.5-explore-picker-ax.png` | `9a832b20f0ed58e6a42c3ca17ff1edf6012c3c5b628d09cac105dbbc0d269d05` |
| `?variant=door-default` | `t2.5-explore-door-default.png` | `91052d119734798da4715181decb649f4eb21782212fd8355e877f12bceb63cb` |
| `?variant=door-active` | `t2.5-explore-door-active.png` | `ecb75419079c7fc251dbd8c3a4ada4ed686bbb74ea4b86bce7441747adf55967` |
