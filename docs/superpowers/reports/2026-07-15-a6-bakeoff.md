# WP-A6 Bake-off Status

Date: 2026-07-16
Corpus: `docs/superpowers/eval/real-malaysia-20260715-golden-kl.tsv`
Scoring config: `pipeline/config/scoring.json`

This report stops at the live bake-off line. No NOUS or Modal live calls were made, and no provider keys were read.

Keyless smoke command:

```bash
uv run --extra dev python -m mt_pipeline.cli llm bakeoff --labeled ../docs/superpowers/eval/real-malaysia-20260715-golden-kl.tsv --config config/scoring.json --k 5
```

Output:

```text
model	provider	precision_at_k_llm_on	precision_at_k_llm_off_baseline	lift	cost_usd	lift_per_usd	injection_resistance	error
fake-curiosity-v1	fake-curiosity-v1	0.6	0.8	-0.20000000000000007	0.0	None	1.0	
```

Live bake-off status: blocked on Rob's provider keys and op-run smoke test. The fake result above is only a harness/teardown/A5-seam smoke, not a model recommendation.
