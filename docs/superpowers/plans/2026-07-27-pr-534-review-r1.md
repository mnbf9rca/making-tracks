# Address PR #534 Review Comments (Round 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the missing level-two evidence that both internal material-control bodies consume the interaction rows from the `MaterialTokenSheet` they receive.

**Architecture:** Keep production code unchanged. Extend the existing AppKit renderer tests to construct `MaterialChipStyleBody` and `MaterialButtonStyleBody` directly with a synthetic sheet, then observe the sheet’s non-Snow opacity and scale values in rendered output.

**Tech Stack:** Swift 6, SwiftUI, XCTest, AppKit `ImageRenderer`

## Global Constraints

- Work only in the existing `wp-526-interaction-token-rows` linked worktree.
- Modify only `ios/Tests/DesignSystemTests/ControlStylesTests.swift` and this plan/its generated review artifacts.
- Do not change production code: Opus explicitly found the implementation correct.
- Use the existing synthetic rows exactly: `disabledAlpha: 0.23` and `pressScale: 0.87`.
- Cover both internal consumers: `MaterialChipStyleBody` and `MaterialButtonStyleBody`.
- Prove both rows at the body boundary: disabled rendering observes `0.23`; pressed-to-resting geometry observes `0.87`.
- Keep the level-three boundary honest: styles passing `theme.tokens` cannot be distinguished from hard-coded Snow until a second material exists.
- Preserve all unrelated `0.46` coverage-mask and `0.98` UI-coordinate literals.
- Use `./scripts/sim-lock.sh` for every simulator operation; this focused package test requires no simulator.
- Commit with signing enabled and do not push.

---

### Task 1: Cover interaction-token wiring in both internal bodies

**Files:**
- Modify: `ios/Tests/DesignSystemTests/ControlStylesTests.swift`

**Interfaces:**
- Consumes: `makeInteractionSheet(disabledAlpha:pressScale:)`, `MaterialChipStyleBody`, `MaterialButtonStyleBody`, `render(_:)`, and the existing accent-bound/pixel helpers.
- Produces: body-level regression tests whose names say that each body reads the provided interaction rows.

- [x] **Step 1: Add the body-level tests and renderer inputs**

Under `#if canImport(AppKit)`, add one accurately named test for each body. Each test must:

1. create `makeInteractionSheet(disabledAlpha: 0.23, pressScale: 0.87)`;
2. construct the internal body directly with that sheet;
3. render resting and pressed states and assert the accent-bound width and height ratios are `0.87` with `0.01` accuracy;
4. render the disabled state over transparency and assert the center accent alpha is `0.23` with `0.01` accuracy.

Parameterize the existing `renderChipStyleBody` and `renderButtonStyleBody` helpers, or add narrowly scoped equivalents, so the tests pass the synthetic sheet and disabled environment into the body itself. Keep the existing Snow tests behaviorally unchanged.

- [x] **Step 2: Run the focused tests**

Run:

```bash
swift test --package-path ios --filter ControlStylesTests
```

Expected: the new tests and all existing `ControlStylesTests` pass.

- [x] **Step 3: Prove the tests have teeth**

Temporarily replace each body’s use of its `tokens` property in the opacity and scale calls with `MaterialTheme.snow.tokens`. Re-run only the two new tests.

Expected: both new tests fail on the synthetic `0.23` / `0.87` expectations.

Restore `ios/Sources/DesignSystem/ControlStyles.swift` with `apply_patch`, confirm the production diff is empty, and rerun the two new tests.

Expected: both tests pass after restoration.

- [x] **Step 4: Verify and commit**

Run:

```bash
swift test --package-path ios
git diff --check
git status --short
```

Expected: the full package suite passes, the diff check is clean, and only the planned test/plan files are changed.

Commit:

```bash
git add docs/superpowers/plans/2026-07-27-pr-534-review-r1.md ios/Tests/DesignSystemTests/ControlStylesTests.swift
git commit -S -m "test(ios): cover interaction token body wiring"
```
