# WP-B9 — Search: name lookup + geocoding, LOCAL (privacy)

**Status:** design (opus). Adversarial-gate → PR to `develop`, `sourcery-review`. Pipeline → `develop`,
app → `ios`. Thread `p2p/fable__opus`. Spec §5.3/B9 (#20). Depends on B3 (tile client, built).

## Why

Spec: *"name lookup over region place data + geocoding to jump the map — 'Edinburgh next month' must have
an entry point that isn't panning across the planet. Simple is fine; absent is not."* The design tension:
the app only holds the **current viewport's** tiles (+ visited/saved snapshots), so full-region name
search needs a **published index** — and the privacy-clean way is a **local** search (queries never leave
the device).

## Grounded facts (recon: app `origin/ios`; contracts/pipeline `develop`; cited)

- **Search is entirely GREENFIELD** — no search screen, no geocoder, no `CLGeocoder`, no search index
  (grep-empty on both the app and a published-artifact schema).
- **Local name availability is a SUBSET only:** `place_snapshots.name` (visited/saved places) +
  `lists.name` + the **current viewport's loaded tiles**. There is **no** local corpus of the region's
  place names → full-region search needs a new published artifact.
- **The geocoding gazetteer is DESIGNED (#177 WP-RM-P) but NOT BUILT [gate — correction].** #177's
  `WP-RM-P` designs an OSM `boundary=administrative` **sub-extractor** (names + centroids), but it is
  unmerged **and** requires a **new OSM tag filter** — `osm_candidate_tags.json` today whitelists only
  `historic`/`tourism`/`memorial`/`man_made`, **no `place=`/`admin_level`/`boundary`** — so "Edinburgh"
  (a city / admin relation) is currently extracted by **nothing**. So geocoding-by-zone **BLOCKS on
  WP-RM-P shipping**; **v1 = place-name search over the published places**, geocoding-by-zone follows
  (sequenced after WP-RM-P). *(The earlier draft mis-stated this as a grounded fact.)*
- **Rob's ruling (#177): packs bundle "everything."** The search index **joins the pack**, but — like the
  other sidecars — it is listed in the **versioned PACK-DESCRIPTOR file, NOT the frozen manifest**
  (`manifest.schema` is `const 1` + `additionalProperties:false`; sidecars can't ride it, region-model §6).
- **The pack-descriptor is RATIFIED but UNBUILT [gate — the honest build chain].** #177 (WP-RM) is
  **merged as a design**, but the descriptor **exists in no schema, publisher, or app-side reader on
  `develop`** — WP-RM-P (the pipeline catalog + descriptor emitter) and its app-side reader are the
  **first build**, not yet commissioned. So B9's real dependency chain is: **#177 ratified (done) →
  WP-RM-P descriptor emitter + app-side reader built → WP-B9-P (index into the descriptor) → WP-B9-B (read
  it)**. The WP table carries these as explicit unbuilt deps; there is **no interim descriptor-free
  delivery** (a search index with nowhere to ride isn't shippable) — B9 sequences behind WP-RM-P.
- **Hide is MAP-ONLY** — a hidden place still exists and is *findable via search, marked hidden with
  Unhide* (the ruling landed via #170/#175 — **#167 is the still-OPEN issue that framed it**, not a merged
  PR). Category-layer toggles are a *map* filter, not a search filter.

## 1. The published SEARCH INDEX (D1 — new contract)

- **A per-region `search-index`** (new versioned schema, `min_reader_version`, `additionalProperties:false`):
  entries `{name, alt_names[], kind (place | zone), place_id (null for zone entries), lat, lon, tier,
  category}`. **Corpus = the SHIPPED places [gate — pin to `final_places`]:** the index is built from the
  **post-overflow/exclusion `final_places`** the tiles actually carry (not a broader corpus) — else a
  searchable `place_id` exists in **no tile** and its card is permanently unresolvable (§3). Zone entries
  (geocoding) come from WP-RM-P when it ships.
- **`alt_names` must actually be EXTRACTED [gate — it's schema-only today].** `alt_names[]` powers
  cross-script search (find "吉隆坡" / "Kuala Lumpur" / Jawi), but **nothing populates it** — the OSM
  ingest whitelists no `name:*` tags (git-grep empty). WP-B9-P **must add OSM `name:*` / `alt_name` /
  `int_name` extraction** into the place record feeding the index. And the **matcher must tokenize
  non-Latin scripts**: FTS5's default `unicode61` tokenizer does not word-segment CJK (no spaces) and
  folds Arabic/Jawi diacritics unevenly — so either use FTS5 with a trigram tokenizer for CJK substrings
  (or n-gram the CJK fields) and normalize Arabic presentation forms, or the in-memory matcher does script-
  aware folding. Pin the tokenizer choice (§2) so Malaysia's labelled scripts are actually findable.
- **SHARDED for delta [gate — a monolithic per-region file is NOT delta-capable].** A single index file
  changes on essentially every publish (re-scoring/reconcile) → a full re-download, the opposite of
  incremental (and #177 §6 warns against monolithic per-area blobs). So the index is **sharded**
  (**prefix-scoped** shards — folded first 1–2 chars, §2 — each independently `{sha, bytes}`-listed in the
  **pack-descriptor**) → only content-changed shards re-download, deltaing like tiles.
- **Delivery + size [gate — the region can be ~600k places]:**
  - **In the pack** (offline users): the sharded index rides the pack, listed in the **pack-descriptor**
    (not the manifest), delta per shard.
  - **Online (no pack):** a **tier-gated compact** index (notable subset T1–T2 + zone names) fetched
    whole — small. *(NOT prefix-sharded-on-demand: fetching a shard by typed prefix would egress the
    query prefix to the CDN, breaking §2's "no query leaves the device" — so the online index is a whole
    compact download, never per-keystroke shard fetches.)*
  - **Online carriage + integrity + version-pin [gate — the no-pack app had no discovery story].** The
    compact index can't ride the pack-descriptor (pack-scoped) or the frozen manifest, so it is a
    **standalone published object** `{region}/{pv}/search/compact.json` **discovered via the region index**
    (`regions.json` gains a per-region `search_compact: {path, sha256, bytes}`), **sha-verified on fetch**
    like every other object. **It MUST be pinned to the session's `current.json` `publish_version`** —
    fetch `{pv}/search/...` for the *same* `pv` the app booted against, else version skew reintroduces the
    permanent-miss (index lists a `place_id` the live tiles no longer carry, or vice-versa). `min_reader_version`.
  - Untrusted (§5.5): the index is size-capped, `name` SAFE_TEXT + plain-text, `place_id` pattern-checked,
    lat/lon bounded, `kind` enum. Written-last/atomic; versioned.

## 2. Local search — queries NEVER leave the device (D2 — the privacy property)

- **Search runs entirely LOCALLY over the downloaded index** — a prefix + light fuzzy match (folded/
  diacritic-insensitive; matches `name` + `alt_names`), ranked by `(tier, match-quality)`. **The query
  string never hits the network** (search intent + location is highly sensitive; this is a first-class
  privacy property — state it, and it falls out of the static index).
- **Pin the engine + the shard axis [gate — bounds, not options].** **Engine: GRDB FTS5** (not in-memory)
  as the default — an offline UK pack is ~600k places; an in-memory index would hold the whole corpus
  resident, and #151's memory norm forbids an unbounded resident structure. FTS5 keeps the index on disk
  and queries it; **cap the resident set** at the result page (top-N ranked), not the corpus. In-memory is
  the fallback only for a region small enough to bound (state the entry ceiling if used). **Shard axis:
  PREFIX-scoped** (folded first 1–2 chars), not tile-scoped: tile-scoped shards force loading the whole
  corpus to answer one keystroke (every tile can hold an "Edin…" match), defeating the delta; prefix-scoped
  lets a query load only the matching prefix shard(s) **locally from the installed pack** (no network — the
  shard is already on disk, §1). The published sharding is prefix-keyed; FTS5 ingests the loaded shards.
- **The result-TAP does egress, and we argue it honestly, not away [gate — the stronger leak].** Tapping
  a result **jumps the camera and fetches the covering z10 tile of the destination** (§3) — for an
  **online** user (no pack) that is **query-*derived* egress at tile precision**, and a lone GET for a
  distant tile is *more* distinctive than a pan (a pan is a contiguous run; a jump is one outlier). The
  claim is **not** "nothing leaks" — it is: **(a) the fetch is informationally equivalent to panning to
  that place** (the app has always fetched the viewport's tile; a jump fetches the same tile a pan-to
  would, just without the intervening ones — so search adds a shortcut, not a new data category); **(b)
  it is exactly the case #131 cover-traffic covers** — the jump-fetch rides the same decoy budget as any
  sub-region fetch, so the destination tile is not a lone distinguishable GET; **(c) the OFFLINE path
  avoids it entirely** — with the region pack installed, the covering tile is already local, so a
  result-tap is **zero network**. So: online = panning-equivalent + covered by #131; offline = silent.
  Don't soften §3's flow — this is the honest statement of it.
- **No third-party geocoder by default.** "Jump to Edinburgh" is served by the index's **zone/admin
  entries** (WP-RM gazetteer), not Apple's `CLGeocoder` (which would send the query + IP to Apple).
  `CLGeocoder` is at most a **disclosed, opt-in fallback** for a miss — flag the privacy tradeoff; v1
  recommend index-only (honest "no result" beats a silent third-party query).

## 3. Search UX (D3)

- **Entry point = the search PILL → a sheet over the map** (D3 binds to the ratified #176 IA: full-bleed
  map, no tab bar, sheets over the map — NOT a tab or a separate screen). Tapping the pill raises the
  search sheet; type → **live local results** (places with their category icon + a "zone/place" hint).
  Tap a **zone** → recenter the map there.
- **Tap a place → jump, then RESOLVE the card [gate — the data path can't open a card synchronously].**
  The card resolves only from **loaded-viewport tiles or a local snapshot** (`PlaceResolver` → no
  fetch-by-place-id); a searched place is off-viewport and un-visited → `.unavailable` until its tile
  loads. So the flow is: **jump the camera to `lat/lon` → await the covering z10 tile's load → resolve
  the full card from it**, showing a brief **loading state** meanwhile. Because the corpus is pinned to
  `final_places` (§1), the covering tile **always contains the id** (no permanent miss). *(The index
  entry alone can't build the card — it lacks blurb/photo/sources; the card must come from the tile.)*
- **Empty/short-query** shows nothing (or recent); a miss is copy-accurate to what was actually searched:
  an **online** user searches only the **tier-gated compact** (T1–T2 notable subset), so the miss reads
  **"no match among notable places — download the region for full search"**, NOT "no match in <region>"
  (which would falsely imply the full corpus was searched). An **offline** user (full pack) genuinely
  searched the region → "no match in <region>" is correct there. Accessible (Dynamic Type, VoiceOver —
  #163): the result list is the primary accessible path.
- **Recent searches — OFF by default, or a protected local store [gate — privacy].** A persisted query
  history *is* the sensitive "search intent + location" signal §2 keeps off the network, so persisting it
  naively would sweep it into iCloud/Finder backups (off-device). If shipped: **exclude the store from
  backup** (`isExcludedFromBackup`) + a first-unlock data-protection class + a length/TTL cap + a
  **clear-history** control. **Recommend default-off** for v1.

## 4. Interplay — hidden places + category toggles (D4)

- **Hidden places APPEAR in search, marked hidden, with Unhide** (the map-only ruling — hide affects the
  discovery *map*, not findability — landed via #170/#175; #167 is the OPEN framing issue). So search is
  the re-find path Rob's map-only ruling relies on.
  **[gate — the card Unhide is NOT built yet].** What merged (#175 = **WP-HIDE**) is the hidden *state +
  filter* (`hidden_places`, `setHidden`/`unhide`, the map filter); the **card's Unhide button is
  WP-HIDE-UX**, which is **unbuilt** (`PinState.hidden` + `unhide()` plumbing exist, but no card control
  invokes them). So B9 either **sequences after WP-HIDE-UX** or **scopes the card Unhide into WP-B9-B**
  (the plumbing exists; add the button in the hidden state). Don't assume it's already there.
- **Category-layer toggles do NOT filter search** — they are a *map* declutter (a style-layer filter,
  #170), not a statement about findability. Search is global across categories. (A search-side category
  facet is a possible later refinement, not v1.)

## Build-WP decomposition

| WP | side | scope | depends on |
|---|---|---|---|
| **WP-B9-P** search index (pipeline) | **pipeline (`develop`)** | add OSM `name:*`/`alt_name`/`int_name` extraction (→ `alt_names`, §1); publish the per-region **prefix-sharded** `search-index` over **`final_places`** names + `alt_names` + lat/lon/tier/category; the **online tier-gated compact** as a standalone object discovered via `regions.json` + sha (§1); **sharded, listed in the pack-DESCRIPTOR** (not the manifest); schema + caps + fixtures. **Zone/geo entries deferred to WP-B9-Pgeo** | places (built); **WP-RM-P descriptor emitter (#177 ratified, UNBUILT)** |
| **WP-B9-Pgeo** geocoding gazetteer | **pipeline (`develop`)** | **needs WP-RM-P** (OSM `boundary=administrative` sub-extractor + a **new** `place=`/`admin_level`/`boundary` tag filter — not in `osm_candidate_tags` today) → zone names+centroids into the index | **WP-RM-P (#177 ratified, UNBUILT)** |
| **WP-B9-B** search UX (app) | **app (`ios`)** | read the prefix-sharded index (+ version-pin the online compact to `current.json` `publish_version`, §1); **local FTS5** search, resident set capped to the result page (queries never leave the device); **the search pill → sheet (#176 IA)**; **jump → await covering tile → resolve card** (loading state); miss copy online-vs-offline (§3); recent-searches default-off/protected; category-toggles-don't-filter; a11y. **Card Unhide is WP-HIDE-UX** (sequence after it, or scope the button here) | WP-B9-P; B3 (built); **WP-RM-P app-side descriptor reader (UNBUILT)**; WP-HIDE-UX (card Unhide) |

## Open flags (fable/Rob)

1. **Geocoding sequencing** — v1 ships **place-name search** now; **geocoding-by-zone ("jump to
   Edinburgh") BLOCKS on WP-RM-P** (OSM boundary extraction + a new tag filter, #177 unbuilt). Confirm
   place-name-first, geo-follows. (No third-party `CLGeocoder` — index-only, for privacy.)
2. **Card Unhide** — sequence WP-B9-B after WP-HIDE-UX, or scope the card Unhide button into WP-B9-B
   (the `unhide()` plumbing exists; only the button is missing). Confirm.
3. **Recent searches** — recommend **default-off** for v1 (else the protected-store posture, §3). Confirm.

## Gate & acceptance

- **Adversarial gate (done): 3 critics + verify, 11 raised, 7 survived, all folded.** The gate caught a
  rushed draft:
  - **"sha-listed in the manifest" was impossible** — the manifest is frozen (`const 1`,
    `additionalProperties:false`); the index rides the **pack-descriptor** (§1). *(Same error was in WP-RM
    #177 + offline.md — corrected there too.)*
  - **Geocoding depended on a phantom/unbuilt extraction** — WP-RM-P (OSM boundaries) is designed in #177,
    not built, and needs a new tag filter; v1 = place-name search, geo follows (§Grounded/flags).
  - **A monolithic index isn't delta-capable** — sharded it (§1).
  - **Jump→card can't resolve synchronously** — jump → await covering tile → resolve, corpus pinned to
    `final_places` (§3).
  - **The card Unhide doesn't exist yet** (it's WP-HIDE-UX) — corrected (§4).
  - **Recent searches** at-rest exposure — default-off/protected (§3).
  The local-search privacy spine + the published-index architecture survived; the folds were the delivery
  container, the geo dependency honesty, and the app-side resolution/sequencing.
- **fable fallback review (round 2, 7 findings, all folded):**
  - **Privacy argument gap** — "no query hits the network" ignored that the result-TAP fetches the
    destination tile (query-*derived* egress; a lone distant GET is *more* distinctive than a pan). Argued
    it honestly, not away: online = panning-equivalent + covered by #131 cover-traffic; offline = zero
    network (§2).
  - **Build-chain honesty** — the pack-descriptor is #177-**ratified but UNBUILT** (no schema/publisher/
    reader on `develop`); stated the real chain (#177 → WP-RM-P emitter+reader → WP-B9-P → WP-B9-B), no
    interim descriptor-free delivery (§Grounded + WP table deps).
  - **Online index carriage** — the no-pack compact now has a carrier (standalone object via `regions.json`
    + sha) and is **version-pinned to `current.json` `publish_version`** (else skew → permanent-miss); miss
    copy fixed to "no match among notable places — download for full search" (§1/§3).
  - **`alt_names` was schema-only** — scoped OSM `name:*` extraction into WP-B9-P + non-Latin (CJK/Jawi)
    tokenizer choice (§1/§2).
  - **IA conflict** — "a tab / Search screen" violated #176; bound to **search pill → sheet** (§3).
  - **Unpinned bounds** — pinned **FTS5** (not in-memory; #151 memory norm) with the resident set capped to
    the result page, and **prefix-scoped** shards (tile-scoped would scan the whole corpus per keystroke) (§2).
  - **Cites** — monolithic-blob warning is #177 §6 (not §7); #167 is the OPEN framing issue (ruling landed
    via #170/#175), not a merged PR.
- PR → `develop`, `sourcery-review` only, report `p2p/fable__opus`. No self-merge; fable reviews; `main`
  is Rob's.
