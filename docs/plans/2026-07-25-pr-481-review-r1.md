# PR #481 review round 1 implementation plan

## Context

Address the five technically verified findings in the Opus design review of T1.1.
The ratified design-system spec is authoritative where the derived Phase 1 task
graph incorrectly widens the constant pin block to include the track line. The
reviewer is correcting that graph separately.

## Global constraints

- Only `pin` and `pinFaded` are constant across materials. `trackLine` is a
  per-material token row; Snow keeps `#2D8C83`.
- The token contract must feed MapLibre style-string fields without losing
  translucent token opacity. Opaque output is `#RRGGBB`; translucent output is
  `rgba(r, g, b, a)`.
- Do not add Mud values, migrate `MapTheme`, or change map rendering in T1.1.
- `shadow` remains Snow `rgba(43,40,35,0.10)`. Document that it selects the
  frozen render's `--shadow-soft` recipe; the `0.12` sheet recipe remains
  component-specific.
- The label contrast gate proves the label/halo pair because `PaperStyle`
  always supplies the halo; do not invent direct label-vs-every-fill gates.
- The compiled app accent asset must be compared to
  `MaterialTheme.snow.tokens.accent`, while retaining the global build-setting
  assertion.
- Use test-driven development, keep all existing exact-value and static-color
  gates, and commit implementation changes.

## Task 1: Correct the cross-task token contract

1. Add failing `DesignSystemTests` that require `trackLine` to be an independent
   `MaterialTokenSheet` row and require stable map-style color serialization for
   both opaque and translucent colors.
2. Move `trackLine` out of `PinTokenBlock` into `SemanticColorToken` and
   `MaterialTokenSheet`, preserving Snow `#2D8C83`.
3. Add the minimal public alpha-preserving style-string accessor to
   `MaterialColor`.
4. Run the focused DesignSystem tests, self-review, and commit.

## Task 2: Make review assumptions and drift gates explicit

1. Mark the Snow shadow test value as the chosen frozen-render soft-shadow
   recipe, not a numeric prose-spec value.
2. Comment the label/halo assertion's exact guarantee.
3. Import `DesignSystem` in the hosted app test and derive the AccentColor
   expected RGB from the Snow accent token.
4. Update `/private/tmp/wp-467-pr-body.md` so the contract description matches
   Task 1 and add a `## Taste guesses` section naming the `0.12` alternative.
5. Run the focused DesignSystem and hosted app accent tests, self-review, and
   commit tracked changes.

## Completion

Run a task-scoped review after each task and a final whole-change review. Then
run the full host suite and mandatory locked release gate, update the PR body,
push, reply to the review, and request Opus re-review.
